<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node.js News</title>
  <link rel="stylesheet" type="text/css" href="styles/main.css">

</head>
<body>
  <!-- <h1>Node.JS</h1> -->
  <header class="header">
    <div class="header__logo">News App</div>
    <div class="header__search">
        <form action="/" method="POST">
            <input type="text" name="search" placeholder="Search for News" required>
            <input type="submit" value="Search">
        </form>
    </div>
  </header>
  <div class="wrapper">
    <div class="news">
        <%if(locals.json){%>
        <% for(let i=0;i<json.articles.length;i++){%>
            <a href="<%=json.articles[i].url%>" class="news__card">
                <img src="<%=json.articles[i].urlToImage%>" alt="<%=json.articles[i].title%>">
                <h2><%=json.articles[i].title%></h2>
                <p><%=json.articles[i].description%></p>
            </a>
        <%}%>
    <%}else{%>
        No Post Found
        <%}%>
    </div>
  </div>
  
</body>
</html>